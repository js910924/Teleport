variables:
  PROJECT_NAME: Teleport

stages:
  - build
  - test
  - staging
  - production

build:
  stage: build
  image: mcr.microsoft.com/dotnet/sdk:5.0-buster-slim
  script:
    - "dotnet build"

test:
  stage: test
  script:
	- "dotnet test ./$PROJECT_NAME.UnitTests --no-restore"
